name: my_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: any  # Moved http out of flutter dependency block
  provider: any
  google_sign_in: ^5.2.1
  intl: ^0.19.0
  google_fonts: ^6.2.1
  google_generative_ai: ^0.4.6
  animated_text_kit: ^4.2.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
